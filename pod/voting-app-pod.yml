apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: demo-voting-app
spec:
  containers:
    - name: demo-voteing-app
      image: eesprit/voting-app-vote
      ports:
        - containerPort: 80
